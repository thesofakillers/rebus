[project]
name = "rebus"
version = "0.1.0"
description = "A truly multi-modal language-vision cloze test"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.3.4",
    "ruff>=0.8.4",
]
