[project]
name = "rebus"
version = "0.1.0"
description = "A truly multi-modal language-vision cloze test"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "nltk>=3.9.1",
]

[dependency-groups]
dev = [
    "ipython>=8.31.0",
    "pytest>=8.3.4",
    "ruff>=0.8.4",
]
